name: persona
description: "A Personal Task Manager application with clean architecture."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod:
  riverpod_annotation: 
  
  # Data Persistence
  hive:
  hive_flutter:
  
  # Code Generation
  freezed_annotation: 
  json_annotation:
  
  # Utilities
  uuid: 
  intl: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # Code Generation
  build_runner:
  riverpod_generator: 
  freezed: 
  json_serializable: 
  hive_generator: 

flutter:
  uses-material-design: true
